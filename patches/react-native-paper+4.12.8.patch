diff --git a/node_modules/react-native-paper/src/components/Modal.tsx b/node_modules/react-native-paper/src/components/Modal.tsx
index 89734d0..fb53508 100644
--- a/node_modules/react-native-paper/src/components/Modal.tsx
+++ b/node_modules/react-native-paper/src/components/Modal.tsx
@@ -156,11 +156,10 @@ function Modal({
   };
 
   const removeListeners = () => {
-    if (subscription.current?.remove) {
-      subscription.current?.remove();
-    } else {
-      BackHandler.removeEventListener('hardwareBackPress', handleBack);
-    }
+    // RN 0.79 removed BackHandler.removeEventListener; the subscription is the
+    // only removal mechanism, and when none exists there is nothing to remove.
+    subscription.current?.remove();
+    subscription.current = undefined;
   };
 
   const hideModal = () => {
diff --git a/node_modules/react-native-paper/src/components/TouchableRipple/TouchableRipple.native.tsx b/node_modules/react-native-paper/src/components/TouchableRipple/TouchableRipple.native.tsx
index 2342881..2e0752a 100644
--- a/node_modules/react-native-paper/src/components/TouchableRipple/TouchableRipple.native.tsx
+++ b/node_modules/react-native-paper/src/components/TouchableRipple/TouchableRipple.native.tsx
@@ -14,7 +14,7 @@ import color from 'color';
 import { withTheme } from '../../core/theming';
 
 const ANDROID_VERSION_LOLLIPOP = 21;
-const ANDROID_VERSION_PIE = 28;
+const ANDROID_VERSION_MARSHMALLOW = 23;
 
 type Props = React.ComponentProps<typeof TouchableWithoutFeedback> & {
   borderless?: boolean;
@@ -48,12 +48,12 @@ const TouchableRipple = ({
       .rgb()
       .string();
 
-  // A workaround for ripple on Android P is to use useForeground + overflow: 'hidden'
+  // Draw the ripple in the foreground on all Android touchables, not just
+  // borderless ones. On RN 0.81 the bounded *background* ripple no longer
+  // paints, so every TouchableRipple lost its ripple; foreground works.
   // https://github.com/facebook/react-native/issues/6480
   const useForeground =
-    Platform.OS === 'android' &&
-    Platform.Version >= ANDROID_VERSION_PIE &&
-    borderless;
+    Platform.OS === 'android' && Platform.Version >= ANDROID_VERSION_MARSHMALLOW;
 
   if (TouchableRipple.supported) {
     return (
diff --git a/node_modules/react-native-paper/src/utils/useIsKeyboardShown.tsx b/node_modules/react-native-paper/src/utils/useIsKeyboardShown.tsx
index 28d2771..7e1a492 100644
--- a/node_modules/react-native-paper/src/utils/useIsKeyboardShown.tsx
+++ b/node_modules/react-native-paper/src/utils/useIsKeyboardShown.tsx
@@ -21,31 +21,11 @@ export default function useIsKeyboardShown({ onShow, onHide }: Props) {
     }
 
     return () => {
-      if (Platform.OS === 'ios') {
-        if (willShowSubscription?.remove) {
-          willShowSubscription.remove();
-        } else {
-          Keyboard.removeListener('keyboardWillShow', onShow);
-        }
-
-        if (willHideSubscription?.remove) {
-          willHideSubscription.remove();
-        } else {
-          Keyboard.removeListener('keyboardWillHide', onHide);
-        }
-      } else {
-        if (didShowSubscription?.remove) {
-          didShowSubscription.remove();
-        } else {
-          Keyboard.removeListener('keyboardDidShow', onShow);
-        }
-
-        if (didHideSubscription?.remove) {
-          didHideSubscription.remove();
-        } else {
-          Keyboard.removeListener('keyboardDidHide', onHide);
-        }
-      }
+      // RN 0.71 removed Keyboard.removeListener; subscriptions handle removal.
+      willShowSubscription?.remove();
+      willHideSubscription?.remove();
+      didShowSubscription?.remove();
+      didHideSubscription?.remove();
     };
   }, [onHide, onShow]);
 }
